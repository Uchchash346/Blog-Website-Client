import React, { useEffect, useState } from 'react';
import './BlogCards.css'
const BlogCards = () => {
    // const [blogs, setBlogs] = useState([]);
    // useEffect(() => {
    //     fetch('http://localhost:5000/myFirstBlog')
    // })
    return (
        <div>
            hello
        </div>
    );
};

export default BlogCards;